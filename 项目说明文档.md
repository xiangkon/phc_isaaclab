# Unitree G1 机器人RL训练项目说明文档

## 项目概述

class 'phc_isaaclab.tasks.manager_based.phc_isaaclab.robot.g1.velocity_env_cfg.RobotEnvCfg'>


本项目是一个基于Isaac Lab框架的Unitree G1机器人强化学习训练系统，用于训练29自由度的Unitree G1机器人在复杂地形上进行速度跟踪任务。

### 核心特性
- **并行仿真**: 4096个并行环境高效训练
- **复杂地形**: 鹅卵石道路，支持课程学习
- **精细奖励设计**: 20+项奖励函数，涵盖速度跟踪、步态质量、能量效率等
- **课程学习**: 动态调整地形难度和速度命令范围
- **域随机化**: 物理参数随机化提高泛化能力

## 项目结构

### 文件组织
```
phc_isaaclab/tasks/manager_based/phc_isaaclab/robot/g1/
├── __init__.py              # Gymnasium环境注册
├── velocity_env_cfg.py      # 主环境配置
├── agents/
│   └── rsl_rl_ppo_cfg.py    # PPO算法配置
└── mdp/
    ├── rewards.py           # 自定义奖励函数
    ├── observations.py      # 自定义观测函数
    ├── curriculums.py       # 课程学习函数
    └── commands/
        └── velocity_command.py  # 速度命令扩展
```

### 关键配置文件

#### 1. 环境配置 (`velocity_env_cfg.py`)
```python
@configclass
class RobotEnvCfg(ManagerBasedRLEnvCfg):
    """机器人环境配置"""
    scene: RobotSceneCfg = RobotSceneCfg(num_envs=4096)
    observations: ObservationsCfg = ObservationsCfg()
    actions: ActionsCfg = ActionsCfg()
    commands: CommandsCfg = CommandsCfg()
    rewards: RewardsCfg = RewardsCfg()
    terminations: TerminationsCfg = TerminationsCfg()
    events: EventCfg = EventCfg()
    curriculum: CurriculumCfg = CurriculumCfg()
```

#### 2. PPO算法配置 (`agents/rsl_rl_ppo_cfg.py`)
```python
@configclass
class BasePPORunnerCfg(RslRlOnPolicyRunnerCfg):
    num_steps_per_env = 24
    max_iterations = 50000
    policy = RslRlPpoActorCriticCfg(
        actor_hidden_dims=[512, 256, 128],
        critic_hidden_dims=[512, 256, 128],
    )
    algorithm = RslRlPpoAlgorithmCfg(
        learning_rate=1.0e-3,
        clip_param=0.2,
        entropy_coef=0.01,
        gamma=0.99,
    )
```

## 实现逻辑详解

### 1. 状态空间设计
- **基座状态**: 线速度(3D)、角速度(3D)
- **关节状态**: 29个关节的相对位置和速度(58D)
- **命令输入**: 目标线速度(x,y)和角速度(z)(3D)
- **环境信息**: 重力投影向量(3D)
- **历史信息**: 5步历史观测(~1000D)

### 2. 动作空间设计
- **动作类型**: 关节位置目标
- **动作维度**: 29维（对应29个关节）
- **动作范围**: [-1, 1]经过缩放
- **控制频率**: 20Hz (decimation=4, sim.dt=0.005)

### 3. 奖励函数体系

#### 主要任务奖励
- `track_lin_vel_xy` (权重1.0): 跟踪XY平面线速度
- `track_ang_vel_z` (权重0.5): 跟踪Z轴角速度
- `alive` (权重0.15): 存活奖励

#### 稳定性奖励
- `base_linear_velocity` (-2.0): 惩罚Z方向线速度
- `base_angular_velocity` (-0.05): 惩罚XY平面角速度
- `flat_orientation_l2` (-5.0): 惩罚非水平姿态
- `base_height` (-10.0): 惩罚偏离目标高度(0.78m)

#### 效率奖励
- `joint_vel` (-0.001): 惩罚关节速度
- `joint_acc` (-2.5e-7): 惩罚关节加速度
- `energy` (-2e-5): 惩罚能量消耗
- `action_rate` (-0.05): 惩罚动作变化率

#### 步态奖励
- `gait` (0.5): 步态协调性奖励
- `feet_clearance` (1.0): 脚部离地高度奖励
- `feet_slide` (-0.2): 惩罚脚部滑动

### 4. 课程学习机制

#### 速度命令课程
```python
def lin_vel_cmd_levels(env, env_ids, reward_term_name="track_lin_vel_xy"):
    # 根据当前表现调整速度命令范围
    if reward > reward_term.weight * 0.8:
        ranges.lin_vel_x = torch.clamp(
            ranges.lin_vel_x + [-0.1, 0.1],
            limit_ranges.lin_vel_x[0],
            limit_ranges.lin_vel_x[1]
        )
```

#### 地形难度课程
- 初始: 平坦地形
- 渐进: 增加坡度、不规则度
- 最终: 复杂鹅卵石道路

### 5. 域随机化策略

#### 启动时随机化
- 物理材质摩擦系数: (0.3, 1.0)
- 基座质量: ±1.0-3.0kg

#### 重置时随机化
- 基座位姿: x,y∈(-0.5,0.5), yaw∈(-π,π)
- 外力扭矩: 随机推动

#### 间隔随机化
- 定期推动机器人: 速度范围(-0.5,0.5)

## 训练流程

### 1. 环境安装
```bash
# 安装Isaac Lab
# 参考: https://isaac-sim.github.io/IsaacLab/

# 安装项目依赖
python -m pip install -e source/phc_isaaclab

# 验证安装
python scripts/list_envs.py
```

### 2. 数据准备
- 确保Unitree模型文件存在: `unitree_model/G1/29dof/usd/g1_29dof_rev_1_0/`
- 如果没有，需要从Unitree官方获取模型文件

### 3. 训练命令
```bash
# 基础训练
python scripts/rsl_rl/train.py \
  --task=Unitree-G1-29dof-Velocity-v0 \
  --headless \
  --max_iterations=50000

# 带视频录制
python scripts/rsl_rl/train.py \
  --task=Unitree-G1-29dof-Velocity-v0 \
  --headless \
  --video \
  --video_interval=2000

# 指定环境数量
python scripts/rsl_rl/train.py \
  --task=Unitree-G1-29dof-Velocity-v0 \
  --headless \
  --num_envs=2048
```

### 4. 测试命令
```bash
# 零动作代理测试
python scripts/zero_agent.py --task=Unitree-G1-29dof-Velocity-v0

# 随机动作代理测试
python scripts/random_agent.py --task=Unitree-G1-29dof-Velocity-v0
```

## 创建新项目指南

### 1. 复制项目结构
```bash
cp -r source/phc_isaaclab/phc_isaaclab/tasks/manager_based/phc_isaaclab/robot/g1 \
       source/phc_isaaclab/phc_isaaclab/tasks/manager_based/phc_isaaclab/robot/<new_task>
```

### 2. 修改环境配置
1. 重命名 `velocity_env_cfg.py` 为 `<task_name>_env_cfg.py`
2. 更新类名和配置参数
3. 调整奖励函数权重和观测空间

### 3. 更新注册文件
修改 `__init__.py`:
```python
gym.register(
    id="Unitree-G1-29dof-<TaskName>-v0",
    entry_point="isaaclab.envs:ManagerBasedRLEnv",
    kwargs={
        "env_cfg_entry_point": f"{__name__}.<task_name>_env_cfg:<TaskName>EnvCfg",
        "rsl_rl_cfg_entry_point": f"{agents.__name__}.rsl_rl_ppo_cfg:BasePPORunnerCfg",
    },
)
```

### 4. 自定义MDP组件
- 在 `mdp/rewards.py` 中添加新奖励函数
- 在 `mdp/observations.py` 中添加新观测函数
- 在 `mdp/curriculums.py` 中定义课程学习策略

## 故障排除

### 常见问题

#### 1. 训练不收敛
- **检查**: 奖励函数权重是否平衡
- **调整**: 降低学习率，增加批量大小
- **验证**: 使用零动作代理测试环境是否正常

#### 2. 机器人频繁摔倒
- **检查**: 初始地形难度是否过高
- **调整**: 减少稳定性惩罚权重
- **优化**: 增加关节位置限制的宽松度

#### 3. 训练速度慢
- **优化**: 减少环境数量，增加 `decimation`
- **硬件**: 确保使用GPU，检查内存使用
- **配置**: 调整PhysX解算器参数

#### 4. 模型文件缺失
- **解决**: 从Unitree官方获取模型文件
- **验证**: 检查 `unitree_model/` 目录结构

### 性能优化建议

#### 仿真优化
```python
# 调整PhysX参数
self.sim.physx.gpu_max_rigid_patch_count = 10 * 2**15
self.sim.physx.solver_position_iteration_count = 4
self.sim.physx.solver_velocity_iteration_count = 1
```

#### 训练优化
```python
# 调整PPO参数
num_steps_per_env = 32  # 增加批量大小
empirical_normalization = True  # 启用经验归一化
actor_hidden_dims = [256, 128, 64]  # 减小网络规模
```

## 项目扩展

### 1. 添加新机器人
1. 在 `assets/robots/` 中添加机器人配置文件
2. 定义关节参数、执行器配置、初始状态
3. 在环境配置中引用新机器人配置

### 2. 创建新任务类型
1. 定义新的命令生成器（如位置命令、姿态命令）
2. 设计相应的奖励函数
3. 配置任务特定的终止条件

### 3. 集成新RL算法
1. 在 `agents/` 中添加新算法配置
2. 实现算法特定的包装器
3. 更新训练脚本支持新算法

## 监控和评估

### 训练监控
```bash
# 使用TensorBoard查看训练曲线
tensorboard --logdir logs/rsl_rl/

# 训练日志结构
logs/rsl_rl/Unitree-G1-29dof-Velocity-v0/
├── <timestamp>_<run_name>/
│   ├── train/              # 训练日志
│   ├── videos/train/      # 训练视频
│   ├── params/            # 配置文件
│   └── model_<iteration>.pt  # 模型检查点
```

### 性能评估指标
1. **速度跟踪精度**: 命令速度与实际速度的误差
2. **能量效率**: 单位距离的能量消耗
3. **步态质量**: 脚部接触模式与理想步态的匹配度
4. **稳定性**: 基座高度和姿态的方差
5. **泛化能力**: 在不同地形和扰动下的表现

## 总结

本项目提供了一个完整的Unitree G1机器人RL训练框架，具有以下特点：

1. **模块化设计**: 各组件分离，易于修改和扩展
2. **精细奖励设计**: 多层次奖励体系引导学习
3. **课程学习**: 渐进式难度提升提高训练效率
4. **域随机化**: 增强策略的泛化能力
5. **高效训练**: 并行仿真支持大规模训练

通过本项目的框架，可以快速创建新的机器人控制任务，调整训练策略，并扩展到其他机器人平台。

---

*文档版本: 1.0*
*最后更新: 2025年2月2日*
*适用于: Unitree G1 RL 训练项目*